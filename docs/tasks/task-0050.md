---
task_id: task-0050
title: Add an Experimental/Research Mode toggle for not-yet-clinically-established formulas
status: to do
source: "chat 2026-09-15"
parent_task: task-0024
---

# Add an Experimental/Research Mode toggle for not-yet-clinically-established formulas

Alex's own words: "Advanced Approach (Dual-Mode): If you really want to
leverage the cutting-edge science of the Zakharov allosteric model, offer
it as an 'Experimental / Research Mode' toggle. Clearly add a tool-tip
explaining to the user that this is a modern, highly precise academic
model, but that its numbers should not be used to self-diagnose without a
doctor's oversight."

`parent_task: task-0024` because the motivating case (cFT-Z on Hormonal
Pathways) lives there, but the toggle concept itself is app-wide in
ambition — see "Open questions" below on whether it should generalize
beyond that one page/badge.

## The proposal

An opt-in "Experimental / Research Mode" toggle that gates any
formula/index the app implements but does not consider clinically
established (bleeding-edge academic model, not the Vermeulen-grade
default). When on, such a badge/value becomes visible; when off, it stays
hidden (or shown as "Not available" the way `unavailable: true` badges do
today). A tooltip on the toggle (and/or on the gated badge itself) states
plainly that this is a modern, high-precision academic model whose
numbers should not be used for self-diagnosis without a doctor's
oversight.

## Currently nothing to gate — this specs the mechanism only

There is no candidate formula ready to sit behind this toggle yet. The
motivating case, cFT (Zakharov), is still blocked per task-0044's "cFT-Z
(Zakharov)" section and its "Update, 2026-09-15" note: a screenshot
claiming to show the equation was checked (WebFetch plus a PubMed MCP
full-text pull against the article Alex cited, PMC7983478) and the
equation is not in that source — it's an unverified partial cubic, cut
off mid-term, with no confirmed origin. task-0044 concludes Zakharov
"remains genuinely blocked" pending the real primary-source equation.
This task builds the UX/plumbing for a toggle that has nothing to switch
on yet; implementing Zakharov itself stays task-0044's job, and this task
should not be started as a way to unblock it.

## What the code says: evidenceLevel vs. Experimental Mode

`web/src/data/indexDefs.ts`'s `IndexDef.evidenceLevel: string` (declared
`web/src/data/computedIndices.ts:268`) is populated today with exactly
`'heuristic' | 'consensus' | 'guideline'` and rendered as a colored pill
by `ReferenceBookPage.tsx`'s `EvidenceBadge`/`EVIDENCE_BADGE` map
(task-0044 documents this in full, including a vestigial unused `level`
field on the same interface — not this task's concern).

Reasoning on whether "experimental" extends that enum or is a separate
axis:

- `evidenceLevel` grades how well-established the underlying **science**
  is — is this formula's accuracy backed by guideline consensus, general
  clinical consensus, or is it a heuristic approximation? It answers "how
  much should a clinician trust this number," and it's populated per
  index regardless of whether the index is shown by default.
- "Experimental Mode" as Alex describes it is a **UI/product** axis —
  whether to compute-and-show a value at all without the viewer opting
  in first, independent of how it would eventually be graded once shown.
  A formula could in principle be `evidenceLevel: 'heuristic'` and shown
  by default (FAI already is, per task-0044) — heuristic isn't
  automatically experimental. Conversely a formula could be well-graded
  once verified but still ship behind the toggle for a period simply
  because it's new/unfamiliar to users.
- These look orthogonal, not the same axis: `evidenceLevel` is a
  property of the formula's evidence base; "experimental" is a property
  of the app's disclosure/consent posture toward showing it. Recommend
  keeping them as two separate fields rather than overloading
  `evidenceLevel` with a fourth value — collapsing them would force every
  future low-evidence-but-fine-to-show-by-default index (like FAI) to
  either dodge the "experimental" label or trigger a toggle it doesn't
  need.

## Open questions

- **Per-badge or app-wide?** A single global "Experimental Mode" switch
  (like a settings flag) is simpler to build and explain, but a per-badge
  flag (`experimental: true` on a `Badge`/`IndexDef`) is more flexible if
  only some formulas ever need it and others graduate out over time. Lean
  toward: one global toggle that *reveals* every badge/index flagged
  `experimental: true`, rather than N independent per-badge toggles —
  fewer settings to explain, and "Research Mode" reads as a single
  posture the user opts into, not a per-formula checklist. Not decided.
- **Where does it live, persistence-wise?** This is the same open
  question task-0042 is already working through for the albumin-fallback
  setting: is this a calculation-input-adjacent preference (closer to
  `envelopeMeta`, the device-local sex fallback, "how do I want my numbers computed/shown"), a display
  preference (`bloodtests_view_settings_v1`, alongside `compactPanels`),
  or its own persisted key? Cross-reference task-0042 directly before
  deciding — resolving "where do calculation-affecting toggles live" once
  should settle both tasks' placement rather than each inventing its own
  answer.
- **What triggers the tooltip?** Hover on the toggle, a dedicated info
  icon beside it, first-time-only (e.g. shown once on first enabling,
  never again), or persistent text under the toggle whenever it's on?
  Not decided — note that "not for self-diagnosis" is exactly the kind of
  warning that should probably be seen every time the toggle is flipped
  on, not just once, given the stakes Alex named.
- **Does off hide the badge, or also skip the computation?** This is a
  pure display gate, not a privacy or performance concern (worth stating
  explicitly since other toggles in this app sometimes affect what's
  computed) — recommend the value is still computed internally (so
  turning the toggle on/off is instant, no recompute), and only rendering
  is gated, mirroring how `unavailable: true` badges already render
  today when off.
- Does this toggle, once built, retroactively apply to a badge that's
  currently `unavailable: true` the moment it gets a real formula, or is
  that decided formula-by-formula? (Moot for now: as of the 2026-09-15
  update below, Sartorius and Zakharov were both removed outright rather
  than fixed, so no `unavailable: true` cFT badge is left in the column
  to retroactively apply this to.)

## Update 2026-09-15 — motivating example changed: Zakharov badge removed

The `cft-zakharov` badge that motivated this task has been removed from
`HormonalPathwaysView.tsx` entirely (task-0044's "cFT-Z (Zakharov)"
section, final update) rather than left blocked-and-pending — Alex
decided it's a permanent dead end (paywalled, unreproducible, no
legitimate accessible source found after this session's research), not
a future candidate to eventually unlock. That closes off the original
"if Zakharov's equation is ever obtained" framing for good.

Later the same day, `cft-sartorius` was removed outright too, same
rationale (task-0044's "Sartorius removed too" update): its quoted
coefficients also turned out to be unverifiable, found nowhere in the
literature. So the column now carries zero `unavailable: true` badges;
Mazer (below) is the only forward-looking candidate this toggle would
plausibly ever gate.

The Experimental/Research Mode concept itself doesn't need to go with
it, though — two other logged candidates were sitting alongside Zakharov
at the time this task was written, and one of them fits the "cutting-edge
academic model, not for self-diagnosis" framing better than Zakharov ever
did:

- **Nanjee-Wheeler (task-0046)** doesn't fit this toggle. It's a simple,
  source-verified 1985 empirical regression (two inputs, no albumin, no
  paywall, no iterative solving) — task-0046 scopes it as a normal sixth
  badge to add outright, not something needing a disclosure gate.
- **Mazer (task-0049)** fits much better. It's a coupled 5-hormone/3-protein
  iterative equilibrium solver (Newton-Raphson-class numerics, 15
  association constants, needs DHT/E2/estrone/cortisol/CBG as new inputs
  the app doesn't track today) — genuinely "cutting-edge," genuinely more
  complex and less battle-tested than Vermeulen or Ly & Handelsman, and
  task-0049 already flags it as a much bigger undertaking requiring new
  infrastructure. That's the shape of thing this toggle was designed for.

Recommendation: if this task is picked up, reframe its motivating example
from Zakharov to Mazer (task-0049) rather than dropping the task — the
mechanism (opt-in gate + disclaimer tooltip for a not-yet-established
formula) still has a real future user, just not the one that originally
prompted it. Not otherwise deprioritized by this change; still nothing
concrete to wire the toggle to until task-0049's full text and constants
are pulled and a real `fn` exists to gate.

## Update 2026-09-15 (correction) — Nanjee-Wheeler was not "source-verified"

The update above calls Nanjee-Wheeler "a simple, source-verified 1985
empirical regression" as the reason it doesn't need this toggle. That
turned out to be wrong: task-0046 was implemented the same day from a
formula transcribed off an AI chat tool's screenshot, not read directly
from a verified source, and was reverted after follow-up research found
it computed values 6-8x lower than Vermeulen for men — the opposite of
what three independent peer-reviewed papers (Ho 2006, Morris 2004,
Sartorius 2009) report Nanjee-Wheeler should do. See task-0046's own
"Update 2026-09-15 (reverted)" section for the full account. This
doesn't change this task's conclusion (Nanjee-Wheeler still isn't the
kind of "cutting-edge, needs a disclosure gate" formula this toggle was
designed for — task-0046 is back to unimplemented and blocked on
paywalled sources, not something to gate and show with a warning), but
the "source-verified" characterization was premature and is retracted.
