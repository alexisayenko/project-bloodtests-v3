---
task_id: task-0062
title: Add cholesterol/triglyceride molecule icons and a holder-plus-cargo diagram to Lipid Transport
status: done
done_date: 2026-09-16
source: "chat 2026-09-16"
parent_task: task-0060
---

# Add cholesterol/triglyceride molecule icons and a holder-plus-cargo diagram to Lipid Transport

Alex designed two new icons — a cholesterol molecule (reusing the three-ring
steroid motif already embedded in `CarrierIcon`, redrawn outline-only) and a
triglyceride molecule (glycerol backbone with three fatty-acid tails), both
sketched and approved in chat — and asked for two things on Lipid Transport
(`#lipids`, [task-0060](task-0060.md)):

1. A new diagram showing a lipoprotein's structural apolipoprotein connected
   to its two docked cargo chips, mirroring the holder-plus-cargo visual
   language Hormonal Pathways already uses for SHBG holding testosterone
   (`Carrier`, `HormonalPathwaysView.tsx`: a node, a bond, a node).
2. Swapping the flat-colored TRIG/Chol blob regions in the particle-row
   glyphs (`LipidParticleGlyph.tsx`, Data mode) for the new icon shapes,
   still sized off the sourced composition shares.

## New icons

`web/src/components/conditions/customIcons.tsx`, appended under a "Lipid
molecule icons" comment block, both following the file's existing
`IconComponent` shape (`size`/`color`/`strokeWidth` props, `viewBox="0 0 48
48"`):

- **`CholesterolIcon`** — exactly `CarrierIcon`'s three-ring steroid cargo
  path, redrawn as its own icon: outline only (`fill="none"`, `stroke=
  {color}`), no fill, default color `#94a028` (the bile-green Alex specified),
  `strokeWidth` 1.4, `strokeLinejoin="round"`.
- **`TriglycerideIcon`** — a new glyph: glycerol backbone trimmed to its
  three attachment points plus three fatty-acid tails (2x length, per Alex's
  approved sketch), default color `#f9d77a` (matching the app's existing
  `--lipid-trig` token), `strokeWidth` 2, round caps/joins, three small
  filled circles at the backbone's attachment points.

Both take `x`/`y` via their existing `SVGProps<SVGSVGElement>` passthrough
(`{...rest}`), which is what lets them be nested and positioned as ordinary
child `<svg>` elements inside another SVG — used by both call sites below.

## 1. The cargo diagram

`LipidTransportView.tsx`'s new `CargoDiagram` component, rendered once,
directly under the page's `ArtworkNote`, before the particle grid.

**Scoping decision**: one representative particle, not all six. LDL was
picked — a single apoprotein (ApoB-100, no linked second protein the way
Lp(a) has apo(a)), and the most clinically familiar of the six ("bad
cholesterol"), so it reads clearly as the example the caption calls out
rather than needing per-particle disambiguation. The diagram is a static,
non-interactive illustration of the *pattern*, not a live readout for a
specific particle — extending it to all six would duplicate the particle row
below it (which already shows every particle's own apoprotein pill and cargo
chips) rather than adding anything.

Built from the same pieces `Carrier` (`HormonalPathwaysView.tsx`) uses —
`.mc-pathway-anchor`, `.mc-pathway-bond`, `.mc-pathway-bubble`,
`.mc-pathway-caption`, `.mc-pathway-node-label` — with no new CSS: `CarrierIcon`
in the center (sized the same way `HormonalPathwaysView`'s `CARRIER_SIZE`
is, since its drawing likewise spans ~42.6 of its 48-unit viewBox), a bond
on each side to a docked bubble holding `TriglycerideIcon` / `CholesterolIcon`
at molecular-actor size (`SIZE.molecular`, `pathwayShared.ts`). Deliberately
not wired into `LipidAssociations` or the badge/chip system — it is a fixed
explanatory picture, not a live measurement node.

No new CSS classes were added to `web/src/styles/index.css` at all — another
session was concurrently editing that file (a sidebar-toggle restyle), so
the diagram's own layout (row spacing, caption margins, section title) is
done with inline `style` props instead, per the session's own instruction to
avoid shared-file contention where a reasonable inline alternative exists.

## 2. Particle-glyph icon swap

`LipidParticleGlyph.tsx`'s `ParticleGlyph`: the TRIG/Chol regions previously
rendered as a flat-filled organic polygon (`organicRegions()`'s stacked-band
output, area proportional to the sourced share). Now each region renders as:

- a thin dashed outline of that same organic polygon (so the region's own
  extent — the thing that encodes the composition share — stays visible),
  and
- the matching icon (`TriglycerideIcon` / `CholesterolIcon`), centered on
  the region's own bounding box and sized to `min(width, height) * 0.8`
  (floored at 9px so a near-zero share still draws something), via the new
  `regionIcon()` helper.

This keeps the "scaled to fit... proportional sizing" property `organicRegions`
already provided — a bigger composition share still produces a visibly bigger
icon (LDL's ~50% cholesterol share renders a markedly larger `CholesterolIcon`
than its ~10% triglyceride share; HDL's ~5% cholesterol renders barely
visible, by design) — without inventing a second sizing model.

**Color decision**: the particle-row glyphs keep the page's existing teal
(`--lipid-chol`) / yellow (`--lipid-trig`) tokens rather than switching to
the new bile-green cholesterol treatment. Those tokens are already the
page's established palette — the same colors label the Size and composition
table below the particles and the chip dots — so changing only the glyph
icons' color would read as an inconsistency between the icon and everything
around it referencing the same quantity. The bile-green/black-outline
treatment Alex approved is used as specified on the new cargo diagram, which
is a distinct, purpose-built illustration rather than part of the existing
particle-row chrome.

## Verification

`cd web && npx vitest run` — 44 files / 966 passed / 1 skipped (967 total),
up from the baseline 44/965/1 (966); `npx tsc -b` clean.

Added to `test/pathway-views.test.tsx`'s `LipidTransportView` suite:

- extended "switches between artwork and data particles" to assert LDL's
  Data-mode Chol and TRIG regions each render a nested icon `<svg>` (not a
  flat-filled path), with a nonzero, particle-specific width;
- a new test asserting the cargo diagram renders exactly the `TRIG` /
  `ApoB-100` / `Chol` labels, two bubbles and two bonds.

Verified visually in the browser at `#lipids`: the cargo diagram renders
correctly in both Artwork and Data particle modes; Data mode's Chylomicron,
VLDL and LDL glyphs show the yellow triglyceride icon and (LDL) the teal
cholesterol icon at sizes that visibly track their sourced shares (confirmed
via DOM inspection: LDL's Chol icon ~26px vs its TRIG icon ~12px, matching
its ~50%/~10% shares); no console errors.

## Update 2026-09-16: particle row replaced with an endogenous pathway diagram

Alex dropped the six-particle row entirely (the Artwork/Data toggle,
`LipidParticleGlyph.tsx`, `lipidArtwork.ts`, `lipidParticleGeometry.ts`, the
`web/src/assets/lipids/*.svg` set, and their `--lipid-shell`/`--lipid-trig`/
`--lipid-chol`/`--lipid-apo` tokens) in favor of one new diagram: a
**Liver -> VLDL -> IDL -> LDL** endogenous lipoprotein pathway, in two zones
matching Hormonal Pathways' rotated zone-caption convention (`LIVER`,
`BLOOD TRANSPORT`).

- **VLDL** is drawn as the cargo diagram's own TRIG/ApoB-100/Chol row (now
  the only copy of that visual -- the standalone "How a particle carries its
  cargo" section above the particles is gone, folded into this one use),
  grouped under a `VLDL` label. TRIG and Chol sit on bonds meeting ApoB-100
  at a sharp ~60 degree vertex: each bond is drawn as a measured SVG path
  (`LipidAssociations`' new `vldlBond`) between the three nodes' own
  `data-node` rects, pulled back from each node's center by its own radius
  so it meets the circle radially instead of cutting through it. IDL and
  LDL are plain `ChainAnchor` circles (no cargo breakdown), positioned to
  align with ApoB-100 rather than with TRIG/Chol, since they are the same
  particle further down the chain, not new ones -- deliberately left
  unconnected to VLDL or each other (no bond line), since they are stages,
  not linked reactions.
- The liver keeps its docked HMG-CoA reductase; a bare (uncircled)
  `CholesterolIcon`, sized to match VLDL's own docked Chol icon, sits beside
  it as the enzyme's product, positioned absolutely within `.mc-lipid-liver`
  rather than in flow. The liver's own `Liver` name label was dropped as a
  duplicate of the zone's own rotated `LIVER` caption.
- `pathway-views.test.tsx`'s cargo-diagram test now queries
  `[data-node="vldl"]` instead of the retired standalone section, asserting
  the same four labels (`TRIG`, `ApoB-100`, `Chol`, `VLDL`) and the two
  cargo bubbles.

Verification: `npx tsc -b` clean; `npx vitest run` -- 43 files / 962 passed /
1 skipped (963), all green.

## Update 2026-09-17: TRIG/Chol drawn as circle stacks, one shared bond length

Alex asked for the docked TRIG/Chol icons in the VLDL/IDL/LDL chain to be
redrawn as a discrete count of small circles rather than one icon scaled by
size -- so "how much" reads as "how many circles", never as "how big a
blob".

- New `CompoundStack` component: a vertical stack of identical small
  `mc-pathway-bubble` circles, one `TriglycerideIcon` or `CholesterolIcon`
  each, with an optional `overlap` (px) that cascades them diagonally
  instead of stacking edge to edge -- used for every TRIG and Chol stack in
  the chain. `ParticleNode` (formerly `VldlNode`) takes a `trigCount`
  instead of an icon size: VLDL 3, IDL 2, LDL 0 (no TRIG stack at all).
  Chol's own count stays fixed at 2 across every stage, since its absolute
  amount barely changes down the chain -- only its *share* of a shrinking
  particle rises as TRIG leaves, and letting the Chol icon grow stage over
  stage had visually implied the opposite (a real correction Alex caught
  mid-session). These counts are illustrative, not sourced: the app's own
  `lipoprotein-particles.json` only carries % of each particle's own mass,
  and IDL's isn't sourced at all.
- Every bond line -- TRIG-ApoB, Chol-ApoB in the V, and the plain ApoB-Chol
  bond LDL uses with no TRIG -- is tuned to the same ~75px length
  (`VLDL_CARGO_DROP`, `VLDL_CARGO_GAP`, `LDL_CARGO_GAP`), so the chain reads
  as one consistent bond unit rather than particle-specific line lengths;
  `LipidAssociations` computes all three bonds and their dashed outlines in
  one loop over `['vldl', 'idl', 'ldl']` instead of VLDL-only.
- TRIG and Chol icons both render at a fixed size independent of their
  circle's own size: TRIG at 21px (matching `DOCKED_SIZE`, the size a
  hormone icon renders at docked in a carrier bubble on Hormonal Pathways),
  Chol at 28px since its glyph reads smaller than TRIG's at the same
  nominal size.
- `pathway-views.test.tsx`'s VLDL test now asserts the circle counts (3 TRIG
  / 2 Chol for VLDL, 2 TRIG for IDL, no TRIG stack for LDL) instead of a
  fixed two-bubble/two-bond shape.

Verification: `npx tsc -b` clean; `npx vitest run` -- 43 files / 962 passed /
1 skipped (963), all green.

## Update 2026-09-17: 3/2/1 taper for both compounds, plus chylomicron/HDL/Lp(a)

Two follow-ups from the same session.

**Chol tapers 3/2/1 too, not a fixed count.** Checking the circle counts
against a rough mass estimate (density x volume from each particle's
sourced diameter/density range) showed Chol declining by about the same
factor as the circle-count taper implies (VLDL's Chol mass estimate is
roughly 3x IDL's rough interpolation and LDL's), so `ParticleNode` now takes
a `cholCount` per stage (VLDL 3, IDL 2, LDL 1) instead of a shared constant
-- matching TRIG's own 3/2/1. These mass figures are Claude's own
density x volume extrapolation from the sourced diameter/density ranges,
not something Cox/Feingold state directly, and are wide (VLDL's diameter
range alone spans a ~19x volume difference) -- kept as a sanity check on
the illustration, not as new sourced data.

**Chylomicron, HDL and Lp(a) added, deliberately unconnected to the VLDL
chain.** These three aren't stages of VLDL's transformation (chylomicron is
made in the intestine from dietary fat, not the liver; HDL's ApoA-I is
secreted lipid-poor and acquires its cargo later from peripheral tissues,
not packaged the way VLDL is), so drawing a bond arrow from Liver/VLDL to
any of them would misrepresent the biology. Instead:

- `ParticleNode` gained a `holder` prop (the structural apolipoprotein
  label: ApoB-48 for chylomicron, ApoA-I for HDL, ApoB-100 for Lp(a) same
  as the VLDL chain) and an `extraApo` prop for Lp(a)'s second protein --
  a small docked pill (reusing `CarrierIcon` at ~55% size) with a `(a)`
  tag, positioned at the main ApoB-100 icon's corner.
- Chylomicron gets its own zone, "Intestine" (`mc-pathway-band`, same
  rotated-caption convention as Liver/Blood Transport), placed before
  Liver since dietary fat is processed first; no organ artwork, since none
  exists for the intestine yet.
- HDL and Lp(a) render as a second row inside the existing Blood Transport
  zone, below the VLDL/IDL/LDL chain, with their own dashed outlines and
  bond lines but no connecting arrow to the chain.
- `LipidAssociations`' per-particle bond/outline loop now covers all six
  particle ids (`vldl`, `idl`, `ldl`, `chylomicron`, `hdl`, `lpa`) instead
  of the endogenous three.
- Counts (illustrative, not sourced beyond the % composition table): TRIG
  4 / Chol 1 for chylomicron (90%/3% of mass, the most TRIG-dominant
  particle); TRIG 1 / Chol 1 for both HDL (5%/20%, but by far the smallest
  particle in absolute terms) and Lp(a) (no sourced composition, mirrors
  LDL's own count since it's structurally an LDL particle plus apo(a)).

Verification: `npx tsc -b` clean; `npx vitest run` -- 43 files / 962 passed /
1 skipped (963), all green.

## Update 2026-09-17 (3): Size and composition table removed

Alex asked for the "Size and composition" table and its "Sources" section
dropped from `#lipids` entirely -- `CompositionSection` (rendered at the end
of `LipidTransportView.tsx`) and its component file
(`LipidCompositionSection.tsx`) are gone. `data/lipoproteinParticles.ts` and
`lipoprotein-particles.json` stay: `LipidTransportView.tsx` still reads
`LIPOPROTEIN_PARTICLES` for `ALL_PARTICLES`, the particle-id list its badge
targets and association rings are built from, so the data module, its schema
and its `reference-data.test.ts` conformance block are all still live and
unchanged.

## Update 2026-09-17 (4): intestine artwork added

The "no organ artwork, since none exists for the intestine yet" gap above is
closed: `web/public/pathways/intestine.png` (237x256), cropped from Alex's
ChatGPT-generated sheet the same way `liver.png`/`brain-pituitary.png` were
-- background flood-filled from the border by colour distance with a soft
edge, then the sheet's other two shapes (an unrelated muscle-like glyph and
a cell cluster sharing the same source canvas) dropped by keeping only the
topmost-leftmost connected component, before cropping to content with a ~2%
margin and scaling to 256px on the long side. Wired into the Intestine zone
as `INTESTINE_ART`/`Glyph` in `LipidTransportView.tsx`, beside the
chylomicron `ParticleNode` in a new `.mc-lipid-intestine` flex row,
mirroring `LIVER_ART`/`LiverNode`.

## Update 2026-09-17 (5): VLDL -> LPL -> fatty-acids branch, chain arrows, Enterocytes, Peripheral cells

A separate session request, kept apart from the LDL -> Target-cells arrow
(now relabeled Peripheral cells, see below) since it is a different, real
pathway: VLDL sheds triglyceride via lipoprotein lipase (LPL) as it becomes
IDL/LDL, and the freed fatty acids go to muscle for energy and fat tissue
for storage.

- New `LplBranch` in the Blood Transport zone, below the HDL/Lp(a) row (so
  it can't collide with the VLDL/IDL/LDL chain, Peripheral cells, or that
  row): an LPL node reusing the shared `ENZYME_ART` in a docked bubble
  (same convention as HMG-CoA reductase on the liver), a small cluster of
  three `TriglycerideIcon`s standing in for freed fatty acids (illustrative
  reuse -- LPL frees fatty acids *from* triglycerides), and Muscle /
  Adipocytes as their own cell nodes, reusing the same generic
  `CELLS_ART` (`leydig-cells.png`) as Peripheral cells and Enterocytes
  rather than plain text. `LipidAssociations` measures a two-segment arrow
  (VLDL bottom -> LPL's own bubble, LPL's bubble -> the fatty-acid glyph)
  from fresh `data-node="lpl-bubble"`/`"fatty-acids"` rects, re-measured on
  resize like every other arrow on the page.
- `LipoproteinChain`'s VLDL/IDL/LDL row gap widened 44px -> 76px (less
  cramped), and two new short horizontal arrows now render between
  VLDL->IDL and IDL->LDL, anchored to each particle's own outer box edge
  at its `-apo` icon's own height -- through the gap between the boxes, so
  they never cross either particle's TRIG/Chol circles -- making the
  "VLDL sheds triglyceride and becomes IDL, then LDL" prose visible as an
  actual chain rather than three unconnected diagrams.
- The node LDL delivers cholesterol to is relabeled "Peripheral cells"
  (the standard lipidology term), `data-node="target-cells"` kept as-is
  since the existing arrow measurement queries it.
- New `EnterocytesNode` (same `CELLS_ART` cell artwork) added to the
  Intestine zone beside the intestine artwork and the chylomicron
  `ParticleNode`, `data-node="enterocytes"`, no arrow yet -- presence only,
  matching task-0060's "Enterocytes" gut-zone concept.

Verification: `npx tsc -b` clean; `npx eslint` clean; `npx vitest run
test/pathway-views.test.tsx test/reference-data.test.ts` -- 2 files / 96
passed, all green; checked in the running dev server that the new arrows'
measured paths match freshly computed geometry and that none of the new
nodes' bounding rects overlap the existing chain, Peripheral cells, or the
HDL/Lp(a) row.
