---
task_id: task-0054
title: Fix Panel Detail's What's-in-range chart showing "No plottable markers" on showcase data
status: to do
source: "chat 2026-09-15"
---

# Fix Panel Detail's What's-in-range chart showing "No plottable markers" on showcase data

Found while implementing task-0053 (medications timeline lane): Panel
Detail's own "What's in range" tab shows "No plottable markers" with
the current showcase/demo dataset (`data/generateTestData.ts`), even
though All Observations' What's-in-range tab, on the same data, works
fully.

## What was checked

Confirmed pre-existing and unrelated to task-0053's changes — `git
stash` reproduced the same "No plottable markers" state identically on
unmodified `main`. The medication-lane wiring added in task-0053 is
identical and correct in both views; it's simply unexercisable in
Panel Detail until this is fixed.

## Not yet investigated

Root cause unknown — needs digging into why `LabExploreView`'s marker
availability check comes back empty in Panel Detail specifically
(scoped to one panel's markers) but not in All Observations (unscoped,
sees everything). Likely somewhere in how `buildExploreModel` or the
panel-scoping logic filters markers for the "What's in range" picker
in the Panel Detail case.
